<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>使用说明</title>
<style>
body { font-family: "Microsoft YaHei", SimSun, Arial, sans-serif; font-size: 11pt; line-height: 1.55; color: #222; }
h1 { text-align: center; font-size: 22pt; margin: 18pt 0 12pt; }
h2 { font-size: 15pt; margin: 18pt 0 8pt; border-bottom: 1px solid #999; padding-bottom: 3pt; }
p { margin: 6pt 0; }
ul, ol { margin-top: 4pt; margin-bottom: 8pt; }
li { margin: 3pt 0; }
table { border-collapse: collapse; width: 100%; margin: 8pt 0 12pt; }
th, td { border: 1px solid #888; padding: 5pt; vertical-align: top; }
th { background: #eaeaea; font-weight: bold; }
pre { background: #f5f5f5; border: 1px solid #ccc; padding: 8pt; font-family: Consolas, monospace; white-space: pre-wrap; }
</style>
</head>
<body>
<h1>使用说明</h1>
<p>软件名称：Excel 膜通量实验数据处理工具</p>
<p>一句话简介：本工具用于在 Windows 环境下调用 Microsoft Excel COM，读取电子天平导出的 .xls 原始数据，按纯水或污染物实验模式计算液滴质量差、通量、渗透性及归一化结果，并另存为新的 .xlsm 文件。</p>
<h2>1. 软件名称和一句话简介</h2>
<p>软件名称建议使用“Excel 膜通量实验数据处理工具”。它是一个单文件 Python 程序，主程序文件为 process_excel-1.py，面向电子天平导出的膜通量实验数据处理。程序不会覆盖原始 .xls 文件，而是在同目录或指定输出目录生成新的 .xlsm 结果文件。</p>
<h2>2. 适用实验场景</h2>
<ul>
<li>适用于电子天平按固定时间间隔导出的重量数据处理。代码注释和表头显示默认实验时间间隔为 4 秒，但运行时已经允许用户输入“时间s”。</li>
<li>适用于纯水模式：根据每 5 个液滴质量差的平均值生成右侧 G:M 结果区，计算通量和渗透性。</li>
<li>适用于污染物模式：基于每一个 C 列液滴质量差生成 G:M 结果区，并额外生成 O:V 归一化分析区、X:Y 标准十分钟分析区和散点图工作表。</li>
<li>适用于装有 Microsoft Excel 桌面版的 Windows 电脑；不适合没有 Excel COM 的 Linux、普通 CI 或无桌面环境。</li>
</ul>
<h2>3. 运行环境</h2>
<ul>
<li>操作系统：Windows 10 或 Windows 11。</li>
<li>Python：README 建议 Python 3.10 或更高版本。</li>
<li>Office：必须安装 Microsoft Excel 桌面版，因为程序通过 pywin32 调用 Excel COM。</li>
<li>Python 依赖：requirements.txt 当前只有 pywin32&gt;=306。</li>
<li>图形界面：程序使用 tkinter 弹出模式选择、参数输入、文件选择、错误提示和完成提示窗口。</li>
</ul>
<h2>4. 输入文件要求</h2>
<ul>
<li>正式交互流程只接受扩展名为 .xls 的输入文件；测试模式可通过 allow_xlsm_input 额外接受 .xlsm。</li>
<li>输入文件必须存在且是文件，不能是目录。</li>
<li>程序固定查找名为“电子天平输出”的工作表；其他工作表会保留，但不参与计算。</li>
<li>原始数据主要读取 A/B 两列：A 列为时间，B 列为重量或称量数据。</li>
<li>有效数据结束行按 B 列最后一个非空单元格确定。</li>
<li>A/B 列从第 1 行到 B 列最后非空行之间必须连续；中间 A/B 同时为空、A 列为空或 B 列为空都会报错。</li>
<li>A 列时间只用于格式校验和保留，不参与通量公式。可接受 Excel 日期/时间值、Excel 序列数、常见日期时间文本和纯数字文本。</li>
<li>B 列重量必须能解析为有限数字，布尔值、非数字文本、无穷大或 NaN 会报错。</li>
<li>正式计算起点按 B 列开头连续 0 的区段判断：如果开头是非零，从第 1 行开始；如果开头有连续 0，则从这段连续 0 的最后一个 0 所在行开始保留；如果 B 列全是 0，则无法计算。</li>
</ul>
<h2>5. 程序运行流程</h2>
<ol>
<li>启动程序：可双击 exe、运行 py .\process_excel-1.py，或把一个 .xls 文件拖到脚本/exe 上。</li>
<li>选择实验模式：弹出“选择实验模式”窗口，用户勾选“纯水”或“污染物”，然后点击“确定”。如果未选择会提示“请选择模式”。</li>
<li>输入公共实验参数：弹出“请输入实验参数”窗口，输入“时间s”和“膜面积cm2”。默认值来自代码：时间 4，膜面积 12.57。两个值必须为大于 0 的数字。</li>
<li>选择输入文件：如果启动时没有通过命令行或拖拽提供文件，程序会弹出文件选择框，标题为“请选择原始 Excel .xls 文件”。</li>
<li>纯水模式继续输入压力：依次弹出前半段压力和后半段压力输入框，单位为 bar。若两者相同，代码走单一压力分支；若不同，代码走分段压力分支。</li>
<li>污染物模式继续输入参数：先输入运行压力 bar，再输入纯水通量。纯水通量用于归一化基准。</li>
<li>程序通过 Excel COM 只读打开原始工作簿，定位“电子天平输出”工作表，校验 A/B 列并生成结果区。</li>
<li>处理完成后弹出“完成”窗口，显示输入文件、输出文件、实验模式、时间、膜面积、结果行数和相关压力/通量信息。</li>
<li>如果用户取消任一窗口，程序会弹出“已取消”提示并停止。</li>
</ol>
<h2>6. 输出文件说明</h2>
<ul>
<li>输出格式固定为 .xlsm，Excel SaveAs 使用 FileFormat=52，即 xlOpenXMLWorkbookMacroEnabled。</li>
<li>输出文件不会覆盖原始 .xls。程序以只读方式打开输入文件，并另存新文件。</li>
<li>纯水模式输出文件名：原文件名-纯水计算后.xlsm。</li>
<li>污染物模式输出文件名：原文件名-污染物计算后.xlsm。</li>
<li>如果目标文件已存在，会追加时间戳；如果同一秒内仍冲突，会继续追加计数器。</li>
<li>保存前会再次检查目标路径是否已出现文件；如果已存在，停止保存以避免覆盖。</li>
<li>保存后会检查输出文件是否存在且大小大于 0。</li>
<li>污染物模式会额外创建或复用“散点图工作表”，其中写入辅助数据并生成散点图。</li>
</ul>
<h2>7. 纯水模式功能说明</h2>
<p>纯水模式先生成通用计算区域，再根据压力设置生成 G:M 结果区。G:M 的 H 列来自 D 列“每五个取平均”，因此纯水结果行数等于完整 5 点分组数量。</p>
<ul>
<li>单一压力分支：当前半段压力和后半段压力相等时，所有结果行 K 列写入同一个压力，L=H*10*3600/I/J，M=L/K。保存前会计算全部有效 M 值平均值，并写入 M 列最后一个有效结果值的下一行。</li>
<li>分段压力分支：当前半段压力和后半段压力不相等时，程序先读取 H 列结果并自动识别压力切换编号。识别规则为：从第 6 个 H 值开始判断，若当前连续 3 个 H 值平均值小于前面所有 H 值平均值的 85%，则该编号作为切换开始编号。</li>
<li>如果自动识别失败，程序会显示 G/H 数据表，要求用户手动输入“从第几个编号开始切换压力”。</li>
<li>切换编号之前的 K 列使用前半段压力；切换编号及之后使用后半段压力。</li>
<li>最终校验回修：程序计算所有有效 M 值平均值，阈值为平均值 * 1.15；只检查 K 列等于后半段压力的行。如果某行 M 大于阈值，则把该行 K 改为前半段压力并重算 L/M，循环直到没有异常行。</li>
<li>后半段渗透性平均值：最终只统计 K 列仍等于后半段压力的 M 值，平均值写入 M 列最后一个有效结果值的下一行。如果没有任何后半段压力行，该平均值单元格留空。</li>
<li>保存前纯水模式会强制刷新公式，并检查 H/M 结果区已有可读实值，降低空白输出风险。</li>
</ul>
<h2>8. 污染物模式功能说明</h2>
<p>污染物模式也先生成通用 C:E 区域，但 G:M 结果区与纯水不同：污染物模式的 H 列逐行引用 C 列液滴质量差，不使用 D 列每五个平均值。因此污染物 G:M 结果行数通常远多于纯水模式。</p>
<ul>
<li>G:M 基础区：G 为编号，H=C 同行液滴质量差，I 为用户输入时间s，J 为用户输入膜面积cm2，K 为用户输入的单一运行压力，L 和 M 使用与纯水相同的通量/渗透性公式。</li>
<li>O:V 分析区：第 2 行为表头，第 3 行为纯水通量基准。O/Q/S/U 写入用户输入的纯水通量，P/R/T/V 第 3 行归一化结果为 1。</li>
<li>O/P：5点渗透性和5点归一化通量。每 5 个 C 值为一组计算渗透性，末组不足 5 个点时直接丢弃。</li>
<li>Q/R：15组渗透性和15组归一化通量。将全部有效 C 点尽量平均分为 15 组；如果数据点少于 15，则实际组数等于有效点数。不能整除时，前面的组比后面的组多 1 个点。</li>
<li>S/T：10组渗透性和10组归一化通量，分组规则同上。</li>
<li>U/V：5组渗透性和5组归一化通量，分组规则同上。</li>
<li>X:Y 标准十分钟区：要求污染物模式 M 列至少有 150 个渗透性数据；X3 写纯水通量，Y3 为 1；X4:X8 分别对 M3:M32、M33:M62、M63:M92、M93:M122、M123:M152 每 30 个 M 值取平均，Y 列按 X 列除以 X3 归一化。</li>
<li>散点图：污染物模式生成 5 张独立散点图，分别对应 P、R、T、V 和 Y 列归一化通量。X 轴为序号 1、2、3...，Y 轴为对应归一化通量，第一点均来自第 3 行基准值。</li>
</ul>
<h2>9. 主要计算公式</h2>
<table>
<tr><th>区域</th><th>公式或规则</th><th>说明</th></tr>
<tr><td>C 列液滴质量差</td><td>C当前行 = B当前行 - B上一行</td><td>从 C3 开始写入；第一条差值对应正式计算起点后的下一行减上一行。</td></tr>
<tr><td>D 列每五个取平均</td><td>D组末行 = AVERAGE(C组起始:C组结束)</td><td>每 5 个 C 值一组，只在组末行写公式，不足 5 个的末组不输出。</td></tr>
<tr><td>E 列每十个取平均</td><td>E组末行 = AVERAGE(C组起始:C组结束)</td><td>每 10 个 C 值一组，只在组末行写公式，不足 10 个的末组不输出。</td></tr>
<tr><td>G 列编号</td><td>1, 2, 3...</td><td>右侧结果区从第 3 行开始。</td></tr>
<tr><td>H 列重量g（纯水）</td><td>H = 对应 D 列每五个平均值</td><td>纯水 G:M 基于每五点平均。</td></tr>
<tr><td>H 列重量g（污染物）</td><td>H = C 同行液滴质量差</td><td>污染物 G:M 基于每个 C 点。</td></tr>
<tr><td>I 列时间s</td><td>用户输入 runtime_settings.time_seconds</td><td>默认 4，可在运行时修改。</td></tr>
<tr><td>J 列膜面积cm2</td><td>用户输入 runtime_settings.membrane_area_cm2</td><td>默认 12.57，可在运行时修改。</td></tr>
<tr><td>K 列运行压力bar</td><td>纯水：单一或分段压力；污染物：单一运行压力</td><td>压力必须为大于 0 的数字。</td></tr>
<tr><td>L 列通量LMH</td><td>L = H * 10 * 3600 / I / J</td><td>代码中的实际 Excel 公式为 R1C1：=RC[-4]*10*3600/RC[-3]/RC[-2]。</td></tr>
<tr><td>M 列渗透性LMHbar-1</td><td>M = L / K</td><td>代码中的实际 Excel 公式为 R1C1：=RC[-1]/RC[-2]。</td></tr>
<tr><td>污染物 O/Q/S/U 渗透性</td><td>AVERAGE(C范围) * 10 * 3600 / 时间 / 膜面积 / 压力</td><td>直接使用用户输入的时间、膜面积和运行压力。</td></tr>
<tr><td>污染物归一化通量</td><td>当前渗透性 / 同列第 3 行纯水通量基准</td><td>例如 P4=O4/$O$3，R4=Q4/$Q$3。</td></tr>
<tr><td>标准十分钟 X 列</td><td>每 30 个 M 值取平均，共 5 组</td><td>前提是至少 150 个 M 数据。</td></tr>
<tr><td>标准十分钟 Y 列</td><td>Y = X / $X$3</td><td>X3 为纯水通量基准。</td></tr>
</table>
<h2>10. 输出表格列说明</h2>
<table>
<tr><th>列</th><th>表头</th><th>适用模式</th><th>含义</th></tr>
<tr><td>A</td><td>时间</td><td>通用</td><td>原始 A 列时间，主要用于校验和保留。</td></tr>
<tr><td>B</td><td>每隔四秒的液滴重量</td><td>通用</td><td>原始 B 列重量数据。表头仍写“每隔四秒”，但实际公式时间使用用户输入的 I 列时间。</td></tr>
<tr><td>C</td><td>液滴质量差</td><td>通用</td><td>相邻两行 B 列重量差。</td></tr>
<tr><td>D</td><td>每五个取平均</td><td>通用/纯水主要使用</td><td>每 5 个 C 值平均，纯水 H 列引用这里。</td></tr>
<tr><td>E</td><td>每十个取平均</td><td>通用</td><td>每 10 个 C 值平均。</td></tr>
<tr><td>G</td><td>编号</td><td>纯水/污染物</td><td>结果区序号。</td></tr>
<tr><td>H</td><td>重量g</td><td>纯水/污染物</td><td>纯水引用 D 列；污染物引用 C 列。</td></tr>
<tr><td>I</td><td>时间s</td><td>纯水/污染物</td><td>用户输入时间。</td></tr>
<tr><td>J</td><td>膜面积cm2</td><td>纯水/污染物</td><td>用户输入膜面积。</td></tr>
<tr><td>K</td><td>运行压力bar</td><td>纯水/污染物</td><td>纯水为单一或分段压力；污染物为单一压力。</td></tr>
<tr><td>L</td><td>通量LMH</td><td>纯水/污染物</td><td>按 H、I、J 计算。</td></tr>
<tr><td>M</td><td>渗透性LMHbar-1</td><td>纯水/污染物</td><td>按 L/K 计算。纯水末尾可能追加平均值。</td></tr>
<tr><td>O:P</td><td>5点渗透性/5点归一化通量</td><td>污染物</td><td>每 5 个 C 点计算一组，不足 5 点丢弃。</td></tr>
<tr><td>Q:R</td><td>15组渗透性/15组归一化通量</td><td>污染物</td><td>全部有效 C 点尽量平均分成 15 组。</td></tr>
<tr><td>S:T</td><td>10组渗透性/10组归一化通量</td><td>污染物</td><td>全部有效 C 点尽量平均分成 10 组。</td></tr>
<tr><td>U:V</td><td>5组渗透性/5组归一化通量</td><td>污染物</td><td>全部有效 C 点尽量平均分成 5 组。</td></tr>
<tr><td>X:Y</td><td>标准十分钟渗透性/标准十分钟归一化通量</td><td>污染物</td><td>基于 M 列前 150 个渗透性值，每 30 个取平均生成 5 组。</td></tr>
</table>
<h2>11. 错误处理说明</h2>
<ul>
<li>缺少 pywin32：提示安装 pywin32，并建议运行 pywin32_postinstall。</li>
<li>无法启动 Excel COM：提示确认已安装 Microsoft Excel 桌面版，并确认 COM 环境可用。</li>
<li>输入文件错误：文件不存在、不是文件、扩展名不是 .xls 会报错。</li>
<li>工作表错误：找不到“电子天平输出”工作表时，会列出当前工作表名称。</li>
<li>数据连续性错误：A/B 中间空行、A 列空、B 列空都会报错。</li>
<li>数据格式错误：A 列时间格式异常、B 列重量非数字、H/K/M 结果值为空或格式异常都会报错。</li>
<li>有效数据错误：B 列无有效数据、B 列全是 0、有效液滴质量差不足、每五个平均结果为空都会报错。</li>
<li>压力/参数输入错误：时间、膜面积、压力、纯水通量必须为大于 0 的有限数字。</li>
<li>纯水压力切换错误：自动识别失败时转为手动输入；手动输入必须在实际编号范围内。</li>
<li>污染物标准十分钟错误：M 列渗透性数据少于 150 个时，当前代码会报错并中止污染物输出保存。</li>
<li>保存错误：输出路径已存在、目录不可写、文件被占用、SaveAs 失败、保存后文件不存在或大小为 0 都会报错。</li>
<li>用户取消：取消模式、参数、文件、压力或切换编号输入时，程序显示“已取消”并停止。</li>
<li>未知异常：程序会弹出“未知错误”，附带 traceback 详细信息，同时写入 debug_run_log.txt。</li>
</ul>
<h2>12. 打包和使用方式</h2>
<p>依赖安装：</p>
<pre>py -m pip install -r requirements.txt</pre>
<p>如果 pywin32 安装后仍出现 COM 相关问题，可运行：</p>
<pre>py -m pywin32_postinstall -install</pre>
<p>直接运行：</p>
<pre>py .\process_excel-1.py</pre>
<p>安装 PyInstaller：</p>
<pre>py -m pip install pyinstaller</pre>
<p>打包为单文件无控制台 exe：</p>
<pre>py -m PyInstaller --onefile --noconsole .\process_excel-1.py</pre>
<p>打包后可执行文件通常位于 dist\process_excel-1.exe。目标电脑仍必须安装 Microsoft Excel，因为程序需要调用 Excel COM。调试阶段如果需要看到控制台报错，可去掉 --noconsole。</p>
<h2>13. 当前已知风险</h2>
<ul>
<li>中文乱码或 ????：process_excel-1.py 中仍有若干 docstring 和错误提示是 ????，例如 validate_input_file、parse_positive_float、iter_output_candidates、部分 COM 错误提示等。这些会影响用户看到的错误信息。README.md 以 UTF-8 正常读取时中文正常。</li>
<li>纯水模式输出空白风险：当前代码已经通过“保存前不创建 0 字节占位文件”、强制 Calculate、ensure_pure_water_result_area_ready 检查 H/M 值来降低风险；但 C/D 公式本身没有单独逐项校验，主要通过后续 H/M 间接暴露问题。</li>
<li>时间和膜面积传递：代码实际已把用户输入的时间和膜面积写入 G:M 的 I/J 列；污染物 O:V 公式也直接使用用户输入的时间和膜面积；X:Y 标准十分钟基于 M 列结果，间接使用这些参数。</li>
<li>纯水保存前 C/D/H/L/M：H/M 有明确保存前检查，L 通过 M 公式间接参与；C/D 没有专门保存前断言。</li>
<li>污染物模式用户输入：污染物 G:M、O:V 和 X:Y 链路实际使用用户输入的运行压力、纯水通量、时间和膜面积。</li>
<li>批量自测脚本：项目目录存在 batch_self_check.py，最近报告显示纯水和污染物两种模式均成功；但该脚本当前被 .gitignore 忽略，不会上传到 GitHub。</li>
<li>非交互测试模式：主程序存在 TestModeOptions 和 process_with_test_mode，可由 Python 调用注入参数；但没有公开命令行参数界面给普通用户直接运行非交互测试。</li>
<li>日志文件：程序会写 debug_run_log.txt，但它更像调试日志，当前被 .gitignore 忽略；日志体积可能较大，且不是面向普通用户的结构化运行报告。</li>
<li>界面文案：部分表头仍写“每隔四秒”，但运行时已允许输入时间s，可能让用户误以为时间固定为 4 秒。</li>
<li>配置保存：当前不会保存用户常用时间、膜面积、压力、纯水通量等参数，每次运行都需要重新输入。</li>
<li>污染物标准十分钟强制要求：当前污染物模式如果 M 数据不足 150 个会直接报错并阻止保存，这可能与“先输出已有结果”的使用期望不一致。</li>
<li>工作表名称固定：程序固定处理“电子天平输出”工作表；如果电子天平导出的工作表名称变化，会报错。</li>
</ul>
<h2>14. 后续优化建议</h2>
<ul>
<li>修复 process_excel-1.py 中残留的 ???? 错误提示和注释，保证所有用户可见错误信息都是中文。</li>
<li>为 C/D/L 公式增加保存前显式校验，和现有 H/M 校验一起覆盖纯水空白输出风险。</li>
<li>把“每隔四秒的液滴重量”表头改成更中性的“液滴重量”或根据用户输入时间动态生成表头，避免和可输入时间参数冲突。</li>
<li>把 batch_self_check.py 改为正式测试脚本并纳入版本控制，或者明确保持本地调试用途。</li>
<li>增加公开的非交互命令行参数，例如 --mode、--time、--area、--pressure、--pure-water-flux、--output-dir，方便批量处理和自动化测试。</li>
<li>生成面向用户的运行日志或报告文件，记录输入文件、输出文件、模式、参数、结果行数、压力切换编号和异常修正数量。</li>
<li>增加配置文件保存常用参数，例如默认时间、膜面积、常用压力、最近输入目录。</li>
<li>污染物标准十分钟不足 150 个点时，可考虑改为只跳过 X:Y 和标准十分钟图，而不是中止整个污染物输出；是否这样做需要确认业务规则。</li>
<li>提供工作表名称可配置能力，或在只有一个工作表时允许自动使用唯一工作表；是否允许需要确认。</li>
<li>为公式和输出区域增加更系统的单元测试或基于样例 Excel 的回归测试，尤其覆盖纯水单一压力、纯水分段压力、污染物不足 150 点、开头无 0、开头全 0、空行、中文路径等情况。</li>
</ul>
<h2>15. 需要进一步确认的问题</h2>
<ul>
<li>纯水模式中“前半段压力”和“后半段压力”相等时，当前代码自动视为单一压力实验，并输出全部 M 平均值。请确认这是否符合实验规则。</li>
<li>纯水分段压力自动识别规则当前是“当前 3 个 H 均值 &lt; 前面全部 H 均值的 85%”，自动失败后人工输入切换编号。请确认 85%、3 个点、至少前面 5 个点是否为最终规则。</li>
<li>纯水最终回修规则当前是“只检查后半段压力行，若 M &gt; 全部 M 平均值 * 1.15，则改为前半段压力”。请确认 1.15 阈值和回修方向是否正确。</li>
<li>污染物 O/P 的固定 5 点分组会丢弃不足 5 个点的末组；Q/R、S/T、U/V 则尽量平均使用全部 C 点。请确认这两类分组规则是否正是需求。</li>
<li>污染物模式中的“纯水通量”当前作为 O/Q/S/U/X 第 3 行归一化基准使用。请确认它的单位和含义是否应称为“纯水通量”还是“纯水渗透性”。</li>
<li>污染物标准十分钟区当前要求至少 150 个 M 值，不足时中止整个污染物处理。请确认是否应改为给出提示但仍保存 G:M 和 O:V。</li>
<li>A 列时间当前只做格式校验，不参与计算；通量公式使用用户输入的“时间s”。请确认是否应继续这样设计。</li>
<li>程序固定查找“电子天平输出”工作表。请确认是否允许在找不到该名称但工作簿只有一个工作表时自动处理唯一工作表。</li>
<li>是否希望把 batch_self_check.py 从 .gitignore 中移出并作为正式批量自测脚本上传到 GitHub。</li>
<li>本次交付的使用说明文件为 Word 可打开的 HTML 格式 .doc。若你要求传统 Word 97-2003 二进制 .doc，需要在有 Word 或 LibreOffice 的环境中再转换。</li>
</ul>
</body>
</html>
